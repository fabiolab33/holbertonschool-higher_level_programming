#!/usr/bin/python3
# Function to check if a character is lowercase

def islower(c):
    """
    Returns True if character c is lowercase, False otherwise.
    """
    return ord(c) >= 97 and ord(c) <= 122
