# Python - Everything is object

## Background Context
In Python, "Everything is an object." This project explores how Python handles different types of objects, focusing on the concepts of **mutability**, **immutability**, and **object identity**. Understanding these concepts is crucial for avoiding unexpected behaviors when assigning variables or passing arguments to functions.

## Learning Objectives
By the end of this project, I am able to explain:
* What is an object and the difference between a class and an instance.
* The difference between immutable and mutable objects.
* What is a reference, an assignment, and an alias.
* How to identify if two variables point to the same object (`is` vs `==`).
* How to display a variable's identifier (memory address).
* How Python passes variables to functions (Call by Object Reference).


## Task Overview


| Task | File | Description |
| --- | --- | --- |
| 0. Who am I? | `0-answer.txt` | Function to print the type of an object. |
| 1. Where are you? | `1-answer.txt` | Function to get the variable identifier. |
| 2-5. Right count | `2-answer.txt` to `5-answer.txt` | Identity and memory address of integers. |
| 6-9. Strings | `6-answer.txt` to `9-answer.txt` | Equality vs Identity in strings (Interning). |
| 10-13. Lists | `10-answer.txt` to `13-answer.txt` | Equality vs Identity in lists (Mutability). |
| 14-18. Modifications | `14-answer.txt` to `18-answer.txt` | Effects of mutation and reassignment in lists and ints. |
| 19. Copy a list | `19-copy_list.py` | Python script that returns a copy of a list. |
| 20-26. Tuples | `20-answer.txt` to `26-answer.txt` | Tuple creation and edge cases. |
| 27-28. In-place | `27-answer.txt` to `28-answer.txt` | Difference between `a = a + [x]` and `a += [x]`. |
| 29. Blog post | https://medium.com/@fburgos913/python-everything-is-an-object-mutability-identity-and-references-3f15df5bcb82 | Detailed technical article about Python objects. |
