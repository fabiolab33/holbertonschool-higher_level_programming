# Python More Data Structures

This project contains Python scripts that demonstrate various **data structures**, **dictionary/set operations**, and **functional programming concepts** like `map` and `lambda`. Each task focuses on practicing Python fundamentals and writing clean, Pythonic code.

## Files

### 0-square_matrix_simple.py
* Description: Returns a new matrix with each element squared.

* Prototype: 

`def square_matrix_simple(matrix=[]):`

### 1-search_replace.py

* Description: Returns a new list with all occurrences of search replaced by replace.

* Prototype:

`def search_replace(my_list, search, replace):`

### 2-uniq_add.py

* Description: Returns the sum of all unique integers in a list.

* Prototype:

`def uniq_add(my_list=[]):`

### 3-common_elements.py

* Description: Returns a set of elements common to two sets.

* Prototype:

`def common_elements(set_1, set_2):`

### 4-only_diff_elements.py

* Description: Returns a set of all elements present in only one of the two sets.

* Prototype:

`def only_diff_elements(set_1, set_2):`

### 5-number_keys.py

* Description: Returns the number of keys in a dictionary.

* Prototype:

`def number_keys(a_dictionary):`

### 6-print_sorted_dictionary.py

* Description: Prints a dictionary sorted by its keys.

* Prototype:

`def print_sorted_dictionary(a_dictionary):`

### 7-update_dictionary.py

* Description: Replaces or adds a key/value in a dictionary.

* Prototype:

`def update_dictionary(a_dictionary, key, value):`

### 8-simple_delete.py

* Description: Deletes a key in a dictionary if it exists.

* Prototype:

`def simple_delete(a_dictionary, key=""):`

### 9-multiply_by_2.py

* Description: Returns a new dictionary with all values multiplied by 2.

* Prototype:

`def multiply_by_2(a_dictionary):`

### 10-best_score.py

* Description: Returns the key with the highest integer value in a dictionary. Returns None if dictionary is empty or None.

* Prototype:

`def best_score(a_dictionary):`

### 11-multiply_list_map.py

* Description: Returns a new list with all values multiplied by a number using map (no loops allowed).

* Prototype:

`def multiply_list_map(my_list=[], number=0):`

### 12-roman_to_int.py

* Description: Converts a Roman numeral string to an integer. Returns 0 if input is not a string or is None.

* Prototype:

`def roman_to_int(roman_string):`